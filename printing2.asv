%F - pole zero map
sympref('HeavisideAtOrigin', 1);
h = (((0.5).^n.*heaviside(n)) + ((0.75).^n.*heaviside(n-2)));
H = ztrans(h);
[numerator, denominator] = numden(H);
numerator = sym2poly(numerator);
denominator = sym2poly(denomonstor);
ts = 1;
sys = tf(numerator,denominator,ts);
sys = tf(H);
pzmap(sys)
